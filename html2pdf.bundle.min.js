/* Local placeholder for html2pdf.js bundle.
 * IMPORTANT: Replace this file with the real library for full offline PDF export.
 * Download original from:
 * https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js
 * and overwrite this file's contents.
 * This lightweight stub avoids runtime errors and provides user guidance.
 */
(function(){
  if (window.html2pdf) return;
  let warned = false;
  function notify(){
    if (warned) return; warned = true;
    console.warn('[CV Generator] Placeholder html2pdf stub in use. Connect online once to cache real engine or replace file locally.');
    const badge = document.getElementById('pdfEngineStatus');
    if (badge) { badge.textContent = 'Engine: placeholder'; badge.style.color = '#b45309'; }
  }
  window.html2pdf = function(){
    notify();
    return {
      set: function(){ return this; },
      from: function(){ return this; },
      save: function(){
        alert('PDF engine placeholder: go online once or use Print CV → Save as PDF.');
        return Promise.resolve();
      }
    };
  };
})();
